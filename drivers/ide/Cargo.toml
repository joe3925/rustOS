
[package]
name = "ide"
version = "0.1.0"
edition = "2024"
autotests = false

[lib]
name = "ide"
path = "src/lib.rs"
crate-type = ["cdylib"]
test = false

[package.metadata.rustos-driver]
configuration = "src/ide.toml"

[dependencies]
kernel_api = { workspace = true }
