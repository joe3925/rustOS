[package]
name = "virtio"
version = "0.1.0"
edition = "2024"
autotests = false

[lib]
name = "virtio"
path = "src/lib.rs"
crate-type = ["cdylib"]
test = false

[package.metadata.rustos-driver]
configuration = "src/virtio.toml"

[dependencies]
kernel_api = { workspace = true }
spin = { version = "0.10", default-features = false, features = ["mutex"] }
