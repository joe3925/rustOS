[package]
name = "kernel_sync"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
default = ["std"]
std = []

[dependencies]
crossbeam-queue = { version = "0.3.12", default-features = false, features = [
    "alloc",
] }
kernel_types = { path = "../kernel_types" }
