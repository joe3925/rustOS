{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dbg full build",
            "type": "shell",
            "command": "Set-Location '${workspaceFolder}\\drivers'; .\\build_drivers; if (-not $?) { exit 1 }; Set-Location '${workspaceFolder}\\os'; cargo build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }",
            "problemMatcher": []
        },
        {
            "label": "dbg kernel build",
            "type": "shell",
            "command": "Set-Location '${workspaceFolder}\\os'; cargo build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }",
            "problemMatcher": []
        },
        {
            "label": "dbg launch",
            "type": "shell",
            "command": "cd ${workspaceFolder}/target/debug; ./os.exe",
            "problemMatcher": [],
            "isBackground": true
        },
        {
            "label": "dbg full chain",
            "dependsOn": [
                "dbg full build",
                "dbg launch"
            ],
            "dependsOrder": "sequence",
            "problemMatcher": []
        },
        {
            "label": "dbg kernel chain",
            "dependsOn": [
                "dbg kernel build",
                "dbg launch"
            ],
            "dependsOrder": "sequence",
            "problemMatcher": []
        }
    ]
}