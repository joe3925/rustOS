{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dbg build",
            "type": "shell",
            "command": "cd ${workspaceFolder}\\os; cargo build; cd ${workspaceFolder}\\target\\debug; ./os.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}